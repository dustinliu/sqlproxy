syntax = "proto3";
option java_package = "sqlproxy.proto";

import "common.proto";

message Request {
    enum Type {
        UNKNOWN = 0;
        CONNECT = 1;
        CLOSE = 2;
    }

    Type event = 1;
    Meta meta = 2;
}
