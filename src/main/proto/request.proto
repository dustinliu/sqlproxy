syntax = "proto3";
option java_package = "ysqlrelay.proto";

import "common.proto";

message SqlRequest {
    enum Event {
        CONNECT = 0;
        CLOSE = 1;
    }

    string id = 1;
    Session session = 2;
    Event cmd = 3;
    string sql = 4;
}
